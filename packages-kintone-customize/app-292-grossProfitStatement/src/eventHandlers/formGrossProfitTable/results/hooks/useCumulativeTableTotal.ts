import { SummaryContracts } from '../../../helpers/getSummaryContracts';

export const useCumulativeTableTotal = ({
  contractData,
  year,
  periods,
  area,
}: {
  contractData: SummaryContracts[]
  year: string
  periods: string[]
  area: string[]
}) => {

  const result = contractData.reduce((acc, { }) => {


  } );

  return result;

};
