import { AppIds } from 'config';
import { IContracts, KContracts } from 'types';

export const appId = AppIds.contracts;
export type RecordType = IContracts;
export type RecordKey = KContracts;
export const dataIdPadding = 2;