import { AppIds } from 'config';
import { IExternalmembers, KExternalmembers } from 'types';

export const appId = AppIds.externalMembers;
export type RecordType = IExternalmembers;
export type RecordKey = KExternalmembers;
