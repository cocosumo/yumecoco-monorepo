import { AppIds } from 'config';
import { IMaterialsmajor, KMaterialsmajor } from 'types';

export const appId = AppIds.materialsMajor;
export type RecordType = IMaterialsmajor;
export type RecordKey = KMaterialsmajor;