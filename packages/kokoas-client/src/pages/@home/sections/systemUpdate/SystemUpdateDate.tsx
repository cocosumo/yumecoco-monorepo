import { Typography } from '@mui/material';

export const SystemUpdateDate = () => {

  const date = '2022年12月11日';

  return (
    <Typography fontWeight={800} fontSize={20}>
      {date}
    </Typography>
  );
};