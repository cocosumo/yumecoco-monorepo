import { DialogContent } from '@mui/material';
import { TControlledComponent } from '../schema';

export const ORDialogContent: TControlledComponent = ({ 
  control, 
}) => {

  return (
    <DialogContent>
      
    </DialogContent>
  );
};