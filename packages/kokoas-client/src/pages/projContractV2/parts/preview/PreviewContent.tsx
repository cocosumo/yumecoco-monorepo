import { DialogContent } from '@mui/material';

export const PreviewContent = () => {
  return (
    <DialogContent
      sx={{
        height: '100vh',
        overflow: 'hidden',
        p: 0,
      }}
    >
      Helllo
    </DialogContent>
  );
};