import { DialogContent, DialogContentText } from '@mui/material';

export const SaveToAndpadDialogContent = () => {

  return (
    <DialogContent>
      <DialogContentText>
        Andpadへ登録しますか。
      </DialogContentText>
    </DialogContent>
  );
};