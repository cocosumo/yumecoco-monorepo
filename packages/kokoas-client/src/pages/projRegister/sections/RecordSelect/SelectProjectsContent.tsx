import { DialogContent } from '@mui/material';

export const SelectProjectsContent = ({
  custGroupId,
}: {
  custGroupId?: string
}) => {

  const { data: projRecs } = useProj;

  return (
    <DialogContent>

    </DialogContent>
  );
};