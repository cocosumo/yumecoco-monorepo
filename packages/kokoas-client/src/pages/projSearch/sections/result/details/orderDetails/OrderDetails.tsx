import { Stack } from '@mui/material';

export const OrderDetails = ({
  projId,
}:{
  projId: string
}) => {

  return (
    <Stack
      spacing={2}
    >
      
      <h1>
        Order Details
      </h1>
    </Stack>
  );
};