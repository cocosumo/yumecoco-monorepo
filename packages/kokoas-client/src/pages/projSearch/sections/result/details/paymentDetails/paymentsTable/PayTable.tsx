
import { PayTableContainer } from './PayTableContainer';



export const PayTable = (props: any) => {
  const {
    ...otherProps
  } = props;
  return (
    <PayTableContainer
      body={null}
      footer={null}
    />
  );
};