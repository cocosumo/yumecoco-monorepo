import { Card, CardContent } from '@mui/material';

export const EstimatesList = ({
  projId,
}: {
  projId: string
}) => {
  return (
    <Card>
      <CardContent>

      </CardContent>
    </Card>
  );
};