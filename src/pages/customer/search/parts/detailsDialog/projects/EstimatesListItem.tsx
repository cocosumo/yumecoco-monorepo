import { Card, CardContent } from '@mui/material';

export const EstimatesListItem = ({
  tags,
  amount,
  projEstimateId,
  date,
}: {
  tags: string[],
  amount: number,
  projEstimateId: number,
  date: string,
}) => {
  return (
    <Card>
      <CardContent>

      </CardContent>
    </Card>
  );
};